% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_var_prior.R
\name{set_variance_priors_m2}
\alias{set_variance_priors_m2}
\title{Compute MoM-based variance priors for semo (single-exposure, two-outcome) models}
\usage{
set_variance_priors_m2(
  ghat,
  gse,
  Ghat_mat,
  Gse_mat,
  beta0 = NULL,
  K = NULL,
  Kmin = 5,
  Kmax = 20,
  rho12 = 0,
  rho1g = 0,
  rho2g = 0,
  c_gamma = 0.5,
  c_theta = 0.8,
  global_mean_gamma = NULL,
  global_mean_theta = NULL,
  global_Sigma_theta = NULL,
  hybrid = FALSE,
  kappa_hybrid = 5,
  z_thresh = NULL,
  trim = 0.1,
  kappa_gamma = 1,
  kappa_theta = 1
)
}
\arguments{
\item{ghat, gse}{Exposure GWAS effects and SEs.}

\item{Ghat_mat, Gse_mat}{K x 2 matrices of outcome GWAS effects and SEs.}

\item{beta0}{Optional length-2 init for (beta1, beta2); if NULL, GLS init.}

\item{K}{Number of IVs after filtering.}

\item{Kmin, Kmax}{Floor and cap on the effective K used in prior shape.}

\item{rho12}{Sampling correlation between the two outcomes (sample overlap).}

\item{rho1g, rho2g}{Sampling correlations between exposure and each outcome.}

\item{c_gamma, c_theta}{Prior weights per IV (see set_variance_priors).}

\item{global_mean_gamma, global_mean_theta}{Optional EB centers (hybrid).}

\item{hybrid}{Logical; enable hybrid EB if TRUE.}

\item{kappa_hybrid}{Pooling strength for hybrid mode.}

\item{z_thresh}{Optional |Z_gamma| selection threshold (winner's-curse fix).}

\item{trim}{Tail probability for winsorized moments.}

\item{kappa_gamma, kappa_theta}{Conservative inflation multipliers (>=1).}
}
\value{
A list with sublists \code{gamma} (a, b, prior_mean) and
  \code{theta} (nu, Phi, prior_mean — a 2x2 matrix), plus \code{K},
  \code{eta}, \code{beta0}, \code{notes}.
}
\description{
Same MoM/EB strategy as \code{\link{set_variance_priors}}, but the outcome
is bivariate. Returns an inverse-Wishart prior for the 2x2 pleiotropy
covariance Sigma_theta in addition to the gamma prior.
}
\keyword{internal}

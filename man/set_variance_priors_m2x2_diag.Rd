% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_var_prior.R
\name{set_variance_priors_m2x2_diag}
\alias{set_variance_priors_m2x2_diag}
\title{Compute MoM-based variance priors for memo (2 exposures, 2 outcomes)}
\usage{
set_variance_priors_m2x2_diag(
  ghat_mat,
  gse_mat,
  Ghat_mat,
  Gse_mat,
  B0 = NULL,
  K = NULL,
  Kmin = 5,
  Kmax = 20,
  rho12 = 0,
  rho_gg = 0,
  rho_gj = list(c(0, 0), c(0, 0)),
  c_gamma = 0.5,
  c_theta = 0.8,
  global_Sigma_gamma = NULL,
  global_Sigma_theta = NULL,
  hybrid = FALSE,
  kappa_hybrid = 5,
  z_thresh = NULL,
  trim = 0.1,
  kappa_gamma = 1,
  kappa_theta = 1
)
}
\arguments{
\item{ghat_mat, gse_mat}{K x 2 matrices: per-IV exposure effects and SEs
for the two exposures.}

\item{Ghat_mat, Gse_mat}{K x 2 matrices: outcome effects and SEs for
the two outcomes.}

\item{B0}{Optional length-2 vector of starting beta_j; if NULL, GLS init
per (exposure j, outcome j) pair.}

\item{K}{Number of IVs after filtering.}

\item{Kmin, Kmax}{Floor and cap on the effective K used in prior shape.}

\item{rho12}{Sampling correlation between the two outcomes.}

\item{rho_gg}{Sampling correlation between the two exposures.}

\item{rho_gj}{List of length 2; element j is c(rho_g1_outj, rho_g2_outj),
the sampling correlations between exposure j and the two outcomes.}

\item{c_gamma, c_theta}{Prior weights per IV.}

\item{global_Sigma_gamma, global_Sigma_theta}{Optional EB centers (hybrid).}

\item{hybrid}{Logical; enable hybrid EB if TRUE.}

\item{kappa_hybrid}{Pooling strength for hybrid mode.}

\item{z_thresh}{Optional |Z| selection threshold (winner's-curse fix).}

\item{trim}{Tail probability for winsorized moments.}

\item{kappa_gamma, kappa_theta}{Conservative inflation multipliers (>=1).}
}
\value{
A list with sublists \code{gamma} (nu, Phi, prior_mean — a 2x2
  matrix) and \code{theta} (same structure), plus \code{K}, \code{eta},
  \code{B0}, \code{notes}.
}
\description{
Bivariate-exposure version. Returns inverse-Wishart priors for both
the SNP-effect covariance Sigma_gamma and the pleiotropy covariance
Sigma_theta, used internally by the memo Gibbs sampler.
}
\keyword{internal}

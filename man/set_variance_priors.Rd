% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_var_prior.R
\name{set_variance_priors}
\alias{set_variance_priors}
\title{Compute MoM-based variance priors for seso models}
\usage{
set_variance_priors(
  ghat,
  gse,
  Ghat,
  Gse,
  beta0 = NULL,
  K = NULL,
  Kmin = 5,
  Kmax = 20,
  rho_ov = 0,
  c_gamma = 0.5,
  c_theta = 0.8,
  global_mean_gamma = NULL,
  global_mean_theta = NULL,
  hybrid = FALSE,
  kappa_hybrid = 5,
  z_thresh = NULL,
  trim = 0.1,
  kappa_gamma = 1,
  kappa_theta = 1
)
}
\arguments{
\item{ghat, gse}{QTL effects and SEs for the exposure.}

\item{Ghat, Gse}{GWAS effects and SEs for the outcome.}

\item{beta0}{Optional initial beta; if NULL, GLS init on outcome SEs.}

\item{K}{Number of IVs after filtering.}

\item{Kmin, Kmax}{Floor/cap on K when computing prior shape \code{a}.}

\item{rho_ov}{Sampling correlation due to sample overlap, in [-1,1].}

\item{c_gamma, c_theta}{Prior weight per IV (see Details).}

\item{global_mean_gamma, global_mean_theta}{Optional global EB centers (hybrid mode).}

\item{hybrid}{Logical; if TRUE, prior_mean = eta*local + (1-eta)*global.}

\item{kappa_hybrid}{Pooling strength for hybrid mode (>0).}

\item{z_thresh}{Optional |Z_gamma| selection threshold
(winner's-curse truncated-normal correction).}

\item{trim}{Tail probability for winsorized moments.}

\item{kappa_gamma, kappa_theta}{Conservative inflation multipliers (>=1).}
}
\value{
A list with sublists \code{gamma} and \code{theta}, each
  containing \code{mom_local}, \code{a}, \code{b}, \code{prior_mean};
  plus shared \code{K}, \code{eta}, \code{beta0}, \code{notes}.
}
\description{
Computes method-of-moments (MoM) empirical-Bayes priors for the
SNP-effect variance (sigma^2_gamma) and the horizontal-pleiotropy
variance (sigma^2_theta), used internally by \code{\link{fusiomr}}.
}
\keyword{internal}
